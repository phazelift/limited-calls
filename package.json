{
  "name": "limited-calls",
  "version": "0.3.4",
  "description": "Wraps a function so it will only be called once during a set delay time.",
  "main": "limited-calls.min.js",
  "scripts": {
    "uglify": "uglifyjs limited-calls.js -c -m -o limited-calls.min.js",
    "build": "coffee -c --transpile limited-calls.coffee && npm run uglify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/phazelift/limited-calls"
  },
  "keywords": [
    "wrap",
    "function",
    "limit",
    "limited",
    "call",
    "calls"
  ],
  "author": "Dennis Raymondo <phazelift@gmail.com> (https://github.com/phazelift/)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/phazelift/limited-calls/issues"
  },
  "homepage": "https://github.com/phazelift/limited-calls",
  "dependencies": {
    "types.js": "latest"
  },
  "devDependencies": {
    "@babel/core": "latest",
    "@babel/preset-env": "latest"
  }
}
